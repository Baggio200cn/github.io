// Placeholder for future API utilities
export const api = {
  // Future: Export to different platforms
  exportToWeChat: async (content: string) => {
    console.log('Export to WeChat:', content);
  },
  
  exportToToutiao: async (content: string) => {
    console.log('Export to Toutiao:', content);
  },
  
  exportToX: async (content: string) => {
    console.log('Export to X:', content);
  },
  
  exportToXiaohongshu: async (content: string) => {
    console.log('Export to Xiaohongshu:', content);
  },
  
  exportToBilibili: async (content: string) => {
    console.log('Export to Bilibili:', content);
  },
};
